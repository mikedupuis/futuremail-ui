import React from 'react';
import './Header.css';

const Header = (props) => {
	return (
		<div className="Header">
			<p>Futur<span className="Header-special">e</span>mail</p>
		</div>
	);
};

export default Header;
